---
if (Astro.url.pathname === "/") {
    return new Response(null, {
        status: 307,
        headers: {
            'Location': `${Astro.url.origin}/photography/everything`
        }
    });
}
---
