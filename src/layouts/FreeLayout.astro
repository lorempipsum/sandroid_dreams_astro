---
import GalleryLayout from "../components/react/Layout/GalleryLayout.jsx";
const { frontmatter } = Astro.props;

const pageTitle = frontmatter
    ? frontmatter?.title
        ? frontmatter.title
        : Astro.props.title
    : Astro.props.title;
---

<GalleryLayout title={pageTitle} client:load>
    <head><meta charset="utf-8" /></head>
    <slot />
</GalleryLayout>
